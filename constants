#include<iostream>
using namespace std;
int main(){
	const int number = 10; // This number does not change.
	/* Let's try Is it change or not ?
	number = 20
	If you write this command, it will return an error.*/
}
